from django.apps import AppConfig


class TrainingEventsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "training_events"
    verbose_name = "Тренувальні події"
